<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.pinaz993.ledger">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddStudentActivity" />
        <activity android:name=".AddClassActivity" />
        <activity android:name=".ChooseActivity" />
        <activity android:name=".ClassListActivity" />
        <activity android:name=".StudentRecords" />
        <activity
            android:name=".EditStudentsActivity"
            android:label="@string/title_activity_edit_students"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".MainMenu"
            android:label="@string/title_activity_main_menu"
            android:theme="@style/AppTheme" />
        <activity android:name=".EditStudents" />
        <activity
            android:name=".EditStudentsClassesActivity"
            android:label="@string/title_activity_edit_students_classes"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>